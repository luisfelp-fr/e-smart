[theme]
base = "light"
primaryColor = "#2a78d6"
backgroundColor = "#fcfcfb"
secondaryBackgroundColor = "#f4f3ef"
textColor = "#0b0b0b"

[browser]
gatherUsageStats = false

[server]
# 200 MB: um CSV maior que isso, parseado, estoura a RAM do plano gratuito
maxUploadSize = 200
